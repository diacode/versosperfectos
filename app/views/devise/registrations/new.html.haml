- content_for :title do
  VP | Registro

.portlet
  #facebook_connect
    =link_to omniauth_authorize_path(resource_name, "facebook") do
      =image_tag "access/facebook-connect.png"
  .or
  = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
    = devise_error_messages!

    .wrapper

      %div.field    
        = f.email_field :email, :placeholder => "Email"

      %div.field    
        = f.text_field :displayname, :placeholder => "Nombre"

      %div.field
        = f.password_field :password, :placeholder => "Contraseña"

      %div.field
        = f.password_field :password_confirmation, :placeholder => "Repetir contraseña"

      %div= f.submit "Registrarme!"
