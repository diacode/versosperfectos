- artists.each do |artist|
  .artist-item
    = link_to artist_path(artist) do
      = image_tag(artist.portrait_url(:square), :class => "artist img-circle")
    .artist_name
      =link_to artist.name, artist_path(artist)